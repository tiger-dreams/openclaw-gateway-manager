--- a/src/agents/model-fallback.ts
+++ b/src/agents/model-fallback.ts
@@ -210,7 +210,7 @@ function resolveFallbackCandidates(params: {
       continue;
     }
     const resolved = resolveModelRefFromString({
-      raw: String(raw ?? ""),
+      raw: String(raw ?? ""),
       defaultProvider,
       aliasIndex,
     });
@@ -210,7 +210,7 @@ function resolveFallbackCandidates(params: {
       continue;
     }
     const resolved = resolveModelRefFromString({
-      raw: String(raw ?? ""),
+      raw: String(raw ?? ""),
       defaultProvider,
       aliasIndex,
     });
@@ -211,7 +211,7 @@ function resolveFallbackCandidates(params: {
     const resolved = resolveModelRefFromString({
-      raw: String(raw ?? ""),
+      raw: String(raw ?? ""),
       defaultProvider,
       aliasIndex,
     });
-    addCandidate(resolved.ref, true);
+    addCandidate(resolved.ref, false);
   }
 
   if (params.fallbacksOverride === undefined && primary?.provider && primary.model) {
